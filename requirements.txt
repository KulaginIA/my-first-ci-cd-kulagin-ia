# Актуальный Flask — приложение должно работать 
Flask==3.1.3
# --- Намеренно уязвимые зависимости (для демонстрации SCA) --- 
# CVE-2018-18074: утечка заголовка Authorization при редиректе HTTPS -> HTTP 
requests==2.34.2
# CVE-2020-14343: выполнение произвольного кода через full_load 
PyYAML==6.0.3
